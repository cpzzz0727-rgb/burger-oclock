# Burger O'clock - 漢堡店官方網站

美式靈魂、日式細節的漢堡店官方網站，包含單頁式網站和 3D 翻頁電子菜單。

## 功能特色

- 🍔 現代化單頁式官方網站
- 📖 3D 翻頁電子菜單
- 📱 完全響應式設計 (RWD)
- ✨ 流暢的滾動動畫
- 🎨 美式工業風視覺設計

## 技術棧

- React.js
- Vite
- Tailwind CSS
- Framer Motion
- react-pageflip

## 本地開發

```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev

# 建置生產版本
npm run build

# 預覽生產版本
npm run preview
```

## GitHub Pages 部署

本專案已配置自動部署到 GitHub Pages。當您 push 代碼到 `main` 或 `master` 分支時，會自動觸發部署流程。

### 首次部署設置

1. 在 GitHub 倉庫中，前往 **Settings** > **Pages**
2. 在 **Source** 中選擇 **GitHub Actions**
3. 確保倉庫名稱：
   - 如果是 `username.github.io`，網站會部署到根路徑 `https://username.github.io`
   - 如果是其他名稱（如 `burger-oclock`），網站會部署到 `https://username.github.io/burger-oclock/`

### 自動部署

- 推送到 `main` 或 `master` 分支會自動觸發部署
- 也可以手動觸發：前往 **Actions** > **Deploy to GitHub Pages** > **Run workflow**

### 查看部署狀態

前往 **Actions** 標籤頁查看部署進度和狀態。

## 專案結構

```
├── src/
│   ├── components/      # React 組件
│   │   ├── Hero.jsx
│   │   ├── BrandStory.jsx
│   │   ├── Menu.jsx
│   │   ├── FlipbookMenu.jsx
│   │   └── ...
│   ├── data/            # 資料檔案
│   │   └── restaurantData.js
│   ├── pages/           # 頁面組件
│   └── App.jsx          # 主應用組件
├── .github/
│   └── workflows/       # GitHub Actions
│       └── deploy.yml
└── public/              # 靜態資源
```

## 授權

© 2024 Burger O'clock. All rights reserved.
